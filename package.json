{
  "scripts": {
    "get": "flutter pub get",
    "build": "flutter pub run build_runner build",
    "gql:gen": "get-graphql-schema http://localhost:3000/graphql > lib/schema.graphql",
    "icon:gen": "flutter pub run flutter_launcher_icons:main"
  },
  "devDependencies": {
    "get-graphql-schema": "^2.1.2",
    "npm-check-updates": "^16.4.3"
  }
}
